// auto-generated by a js-code-template-generator

import { CLEAR_ASYNC, SET_ERROR, SET_PENDING, SET_SUCCESS } from './types'
import { createAsyncState, PENDING, SUCCESS } from './helpers'

const initialState = {}

function setPending (state, { id }) {
  return {
    ...state,
    [id]: createAsyncState(PENDING)
  }
}

function setSuccess (state, { id }) {
  return {
    ...state,
    [id]: createAsyncState(SUCCESS)
  }
}

function setError (state, { id, err }) {
  return {
    ...state,
    [id]: createAsyncState(id, err)
  }
}

function clearAsync (state, { id }) {
  if (!id) return initialState
  const { [id]: _, ...restOfState } = state
  return restOfState
}

const reducer = {
  [SET_PENDING]: setPending,
  [SET_SUCCESS]: setSuccess,
  [SET_ERROR]: setError,
  [CLEAR_ASYNC]: clearAsync
}
export default (state = initialState, action = {}) =>
  reducer[action.type] ? reducer[action.type](state, action) : state
